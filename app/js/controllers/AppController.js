/**
 * Default controller
 * Remove or rename this for your purpose.
 */
class AppController {
    constructor() {
        this.hello = 'Hello from AppController';
    }
}

/**
 * Export as default to make it importable.
 */
export default AppController;